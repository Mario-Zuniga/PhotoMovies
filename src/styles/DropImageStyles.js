import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  drop: {
    alignItems: "center",
  },
  textArea: {
    textAlign: "center",
    color: "white",
  },
}));

export { useStyles };
